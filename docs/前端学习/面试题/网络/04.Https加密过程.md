# Https 加密过程

利用 SSL/TLS 协议（对称加密和非对称加密），可以实现客户端与服务器之间的通信加密，确保数据传输过程中的信息安全。

## 1. 客户端

客户端向服务器发送请求，请求中包含了客户端支持的协议版本、加密方式、生成随机字符串 A

## 2. 服务器

服务器下发公钥和下发 CA 证书

## 3. 客户端

客户端验证 CA 证书，利用公钥对字符串 A 进行加密

## 4.服务器

服务器利用私钥对字符串 A 进行解密

## 5. 客户端 服务端

双端利用字符串 A 进行通信，进行加密通信
